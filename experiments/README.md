# Experiments Directory

This directory contains legacy exploratory notebooks and compatibility wrappers.

Canonical benchmark logic now lives in:

- `kaira/eval/`
- `eval/run_experiment.py`

These files remain for continuity, but they are not the primary implementation surface.
